apiVersion: batch/v1
kind: Job
metadata:
  name: {{ job_name }}
spec:
  template:
    spec:
      containers:
      - name: {{ params.container_name }}
        image: {{ params.image }}
        command: {{ params.command }}
        env:
{% for key, value in params.env.items() %}
        - name: {{ key }}
          value: {{ value }}
{% endfor %}
      restartPolicy: Never
  backoffLimit: 0
